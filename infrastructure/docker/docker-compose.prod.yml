services:
  backend:
    build:
      context: ../../backend
      target: production
    env_file: #toutes les variables d'env chargees
      - ../../backend/.env 
    environment:
      - NODE_ENV=production

  frontend:
    build:
      context: ../../frontend
      target: production
    # ports:
    #   - "80:80" car nginx va faire le reverse proxy via le port 80
    environment:
      - NODE_ENV=production

  ai:
    build:
      context: ../../ai
      target: production
    environment:
      - ENV=production
    volumes:
      - ../../ai/models:/app/models:ro
  
  nginx:
    volumes:
      - ../nginx/default.prod.conf:/etc/nginx/conf.d/default.conf